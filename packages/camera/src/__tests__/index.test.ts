import { expect } from 'chai';
import * as sinon from 'sinon';

describe('Camera', function () {
    it('should have tests!', function () {
        throw new Error('Camera package has no tests, but should');
    });
});
